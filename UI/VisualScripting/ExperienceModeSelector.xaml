<UserControl x:Class="BasicToMips.UI.VisualScripting.ExperienceModeSelector"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <UserControl.Resources>
        <!-- Mode Button Style -->
        <Style x:Key="ModeButtonStyle" TargetType="RadioButton">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="border"
                                Background="{DynamicResource SecondaryBackgroundBrush}"
                                BorderBrush="{DynamicResource SeparatorBrush}"
                                BorderThickness="1"
                                CornerRadius="4"
                                Padding="8,4"
                                Margin="2,0">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{TemplateBinding Tag}"
                                           FontSize="14"
                                           VerticalAlignment="Center"
                                           Margin="0,0,4,0"/>
                                <TextBlock Text="{TemplateBinding Content}"
                                           VerticalAlignment="Center"
                                           Foreground="White"/>
                            </StackPanel>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#2563EB"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#1E40AF"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#334155"/>
                            </Trigger>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsMouseOver" Value="True"/>
                                    <Condition Property="IsChecked" Value="True"/>
                                </MultiTrigger.Conditions>
                                <Setter TargetName="border" Property="Background" Value="#1E40AF"/>
                            </MultiTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Border Background="{DynamicResource SecondaryBackgroundBrush}"
            BorderBrush="{DynamicResource SeparatorBrush}"
            BorderThickness="1"
            CornerRadius="4"
            Padding="4">
        <StackPanel Orientation="Horizontal">
            <TextBlock Text="Experience:"
                       VerticalAlignment="Center"
                       Margin="4,0,8,0"
                       Foreground="{DynamicResource SecondaryTextBrush}"/>

            <RadioButton x:Name="BeginnerButton"
                         Style="{StaticResource ModeButtonStyle}"
                         Content="Beginner"
                         Tag="🎓"
                         ToolTip="Simple interface with friendly labels and essential nodes"
                         Checked="ModeButton_Checked"/>

            <RadioButton x:Name="IntermediateButton"
                         Style="{StaticResource ModeButtonStyle}"
                         Content="Intermediate"
                         Tag="📊"
                         ToolTip="Balanced interface with code preview and most features"
                         Checked="ModeButton_Checked"/>

            <RadioButton x:Name="ExpertButton"
                         Style="{StaticResource ModeButtonStyle}"
                         Content="Expert"
                         Tag="⚡"
                         ToolTip="Full interface with IC10 code, all nodes, and optimization hints"
                         Checked="ModeButton_Checked"/>

            <RadioButton x:Name="CustomButton"
                         Style="{StaticResource ModeButtonStyle}"
                         Content="Custom"
                         Tag="⚙️"
                         ToolTip="Customize your own experience settings"
                         Checked="ModeButton_Checked"/>

            <Button x:Name="CustomizeButton"
                    Content="⚙️"
                    ToolTip="Customize experience settings..."
                    Margin="4,0,0,0"
                    Padding="8,4"
                    Background="{DynamicResource SecondaryBackgroundBrush}"
                    BorderBrush="{DynamicResource SeparatorBrush}"
                    Foreground="White"
                    Click="CustomizeButton_Click">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border x:Name="border"
                                            Background="{TemplateBinding Background}"
                                            BorderBrush="{TemplateBinding BorderBrush}"
                                            BorderThickness="1"
                                            CornerRadius="4"
                                            Padding="{TemplateBinding Padding}">
                                        <ContentPresenter HorizontalAlignment="Center"
                                                        VerticalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="border" Property="Background" Value="#334155"/>
                                        </Trigger>
                                        <Trigger Property="IsPressed" Value="True">
                                            <Setter TargetName="border" Property="Background" Value="#1E293B"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Button.Style>
            </Button>
        </StackPanel>
    </Border>
</UserControl>
