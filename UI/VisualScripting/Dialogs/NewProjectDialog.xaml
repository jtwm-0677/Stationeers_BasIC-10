<Window x:Class="BasicToMips.UI.VisualScripting.Dialogs.NewProjectDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="New Visual Script Project"
        Width="500" Height="450"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#1E1E1E"
        Foreground="White">

    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#2D2D2D"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#3F3F46"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="6"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>

        <Style TargetType="Button">
            <Setter Property="Background" Value="#3F3F46"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="3">
                            <ContentPresenter HorizontalAlignment="Center"
                                            VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#505050"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="#0E639C"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1177BB"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Project Name -->
        <StackPanel Grid.Row="0" Margin="0,0,0,15">
            <TextBlock Text="Project Name" FontWeight="SemiBold" Margin="0,0,0,5"/>
            <TextBox x:Name="ProjectNameTextBox" Text="MyScript"/>
        </StackPanel>

        <!-- Location -->
        <StackPanel Grid.Row="1" Margin="0,0,0,15">
            <TextBlock Text="Location" FontWeight="SemiBold" Margin="0,0,0,5"/>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox x:Name="LocationTextBox" Grid.Column="0" IsReadOnly="True"/>
                <Button Grid.Column="1" Content="Browse..." Width="80" Margin="5,0,0,0"
                        Click="BrowseButton_Click"/>
            </Grid>
        </StackPanel>

        <!-- Experience Mode -->
        <StackPanel Grid.Row="2" Margin="0,0,0,15">
            <TextBlock Text="Experience Mode" FontWeight="SemiBold" Margin="0,0,0,5"/>
            <ComboBox x:Name="ExperienceModeComboBox"
                      Background="#2D2D2D"
                      Foreground="White"
                      BorderBrush="#3F3F46"
                      SelectedIndex="0">
                <ComboBoxItem Content="Beginner - Simplified UI, friendly labels"/>
                <ComboBoxItem Content="Intermediate - Balanced features"/>
                <ComboBoxItem Content="Expert - All features, technical labels"/>
            </ComboBox>
        </StackPanel>

        <!-- Template Selection -->
        <StackPanel Grid.Row="3" Margin="0,0,0,15">
            <TextBlock Text="Template" FontWeight="SemiBold" Margin="0,0,0,5"/>
            <ListBox x:Name="TemplateListBox"
                     Background="#2D2D2D"
                     Foreground="White"
                     BorderBrush="#3F3F46"
                     Height="120"
                     SelectedIndex="0">
                <ListBoxItem>
                    <StackPanel>
                        <TextBlock Text="Blank Project" FontWeight="SemiBold"/>
                        <TextBlock Text="Empty canvas to start from scratch"
                                   FontSize="11" Foreground="Gray" Margin="0,2,0,0"/>
                    </StackPanel>
                </ListBoxItem>
                <ListBoxItem>
                    <StackPanel>
                        <TextBlock Text="Hello World" FontWeight="SemiBold"/>
                        <TextBlock Text="Simple example: Blink a light on/off"
                                   FontSize="11" Foreground="Gray" Margin="0,2,0,0"/>
                    </StackPanel>
                </ListBoxItem>
                <ListBoxItem>
                    <StackPanel>
                        <TextBlock Text="Sensor Monitor" FontWeight="SemiBold"/>
                        <TextBlock Text="Read sensor values and display them"
                                   FontSize="11" Foreground="Gray" Margin="0,2,0,0"/>
                    </StackPanel>
                </ListBoxItem>
                <ListBoxItem>
                    <StackPanel>
                        <TextBlock Text="Device Controller" FontWeight="SemiBold"/>
                        <TextBlock Text="Control devices based on conditions"
                                   FontSize="11" Foreground="Gray" Margin="0,2,0,0"/>
                    </StackPanel>
                </ListBoxItem>
            </ListBox>
        </StackPanel>

        <!-- Buttons -->
        <Grid Grid.Row="5" Margin="0,15,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="1" Content="Create" Width="100"
                    Style="{StaticResource PrimaryButton}"
                    Click="CreateButton_Click"/>
            <Button Grid.Column="2" Content="Cancel" Width="100" Margin="10,0,0,0"
                    Click="CancelButton_Click"/>
        </Grid>
    </Grid>
</Window>
