<Window x:Class="BasicToMips.UI.DebugConsoleWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Debug Console" Height="400" Width="700"
        Background="#1E1E1E"
        WindowStartupLocation="Manual"
        Left="50" Top="50">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Background="#2D2D30" Margin="0,0,0,2">
            <Button x:Name="ClearButton" Content="Clear" Click="ClearButton_Click"
                    Padding="10,5" Margin="5" Background="#3E3E42" Foreground="#D4D4D4" BorderThickness="0"/>
            <Button x:Name="CopyButton" Content="Copy All" Click="CopyButton_Click"
                    Padding="10,5" Margin="5" Background="#3E3E42" Foreground="#D4D4D4" BorderThickness="0"/>
            <CheckBox x:Name="AutoScrollCheck" Content="Auto-scroll" IsChecked="True"
                      VerticalAlignment="Center" Margin="10,0" Foreground="#D4D4D4"/>
            <CheckBox x:Name="EnableLoggingCheck" Content="Enable Logging" IsChecked="True"
                      Checked="EnableLoggingCheck_Changed" Unchecked="EnableLoggingCheck_Changed"
                      VerticalAlignment="Center" Margin="10,0" Foreground="#D4D4D4"/>
            <TextBlock x:Name="LogCountText" Text="0 entries" VerticalAlignment="Center"
                       Margin="20,0" Foreground="#858585"/>
        </StackPanel>

        <!-- Log Output -->
        <ListBox x:Name="LogList" Grid.Row="1"
                 Background="#1E1E1E" Foreground="#D4D4D4"
                 BorderThickness="0"
                 FontFamily="Consolas" FontSize="11"
                 ScrollViewer.HorizontalScrollBarVisibility="Auto"
                 VirtualizingPanel.IsVirtualizing="True"
                 VirtualizingPanel.VirtualizationMode="Recycling">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding}" TextWrapping="NoWrap">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Source}" Value="ERROR">
                                        <Setter Property="Foreground" Value="#F14C4C"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </DataTemplate>
            </ListBox.ItemTemplate>
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Padding" Value="2,1"/>
                    <Setter Property="Margin" Value="0"/>
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="BorderThickness" Value="0"/>
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="#264F78"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </ListBox.ItemContainerStyle>
        </ListBox>
    </Grid>
</Window>
