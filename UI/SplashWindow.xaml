<Window x:Class="BasicToMips.UI.SplashWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Loading..."
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Width="400" Height="450">

    <Border Background="#1E1E1E" CornerRadius="12" BorderBrush="#3C3C3C" BorderThickness="2">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Logo -->
            <Image Grid.Row="0" Source="/logo.png"
                   Margin="40,40,40,20"
                   RenderOptions.BitmapScalingMode="HighQuality"
                   VerticalAlignment="Center"
                   HorizontalAlignment="Center"/>

            <!-- Loading Text -->
            <StackPanel Grid.Row="1" HorizontalAlignment="Center" Margin="0,0,0,30">
                <TextBlock Text="Stationeers Basic-10"
                           FontSize="18" FontWeight="SemiBold"
                           Foreground="#E0E0E0"
                           HorizontalAlignment="Center"/>
                <TextBlock Text="Loading..."
                           FontSize="12"
                           Foreground="#808080"
                           HorizontalAlignment="Center"
                           Margin="0,8,0,0"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
