<local:WidgetBase x:Class="BasicToMips.UI.Dashboard.Widgets.VariableWatchWidget"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:BasicToMips.UI.Dashboard"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="450">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <Border Grid.Row="0" Background="#252526" Padding="6" BorderBrush="#3F3F46" BorderThickness="0,0,0,1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Add variable input -->
                <TextBox x:Name="AddVariableTextBox"
                         Grid.Column="0"
                         Background="#1E1E1E"
                         Foreground="White"
                         BorderBrush="#3F3F46"
                         BorderThickness="1"
                         Padding="6,4"
                         FontSize="12"
                         VerticalContentAlignment="Center"
                         CaretBrush="White"
                         KeyDown="AddVariableTextBox_KeyDown">
                    <TextBox.Style>
                        <Style TargetType="TextBox">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="TextBox">
                                        <Border Background="{TemplateBinding Background}"
                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                CornerRadius="3">
                                            <Grid>
                                                <ScrollViewer x:Name="PART_ContentHost"
                                                             Padding="{TemplateBinding Padding}"/>
                                                <TextBlock Text="Enter variable name..."
                                                          Foreground="#858585"
                                                          FontSize="12"
                                                          Margin="6,4"
                                                          IsHitTestVisible="False">
                                                    <TextBlock.Style>
                                                        <Style TargetType="TextBlock">
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding Text, RelativeSource={RelativeSource TemplatedParent}}" Value="">
                                                                    <Setter Property="Visibility" Value="Visible"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </TextBlock.Style>
                                                </TextBlock>
                                            </Grid>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </TextBox.Style>
                </TextBox>

                <!-- Add button -->
                <Button x:Name="AddButton"
                        Grid.Column="1"
                        Content="Add"
                        Background="#007ACC"
                        Foreground="White"
                        BorderThickness="0"
                        Padding="12,4"
                        Margin="6,0,0,0"
                        FontSize="12"
                        Cursor="Hand"
                        Click="AddButton_Click">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border x:Name="border"
                                                Background="{TemplateBinding Background}"
                                                CornerRadius="3"
                                                Padding="{TemplateBinding Padding}">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="border" Property="Background" Value="#1C97EA"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>

                <!-- Clear all button -->
                <Button x:Name="ClearAllButton"
                        Grid.Column="2"
                        Content="Clear"
                        Background="#3F3F46"
                        Foreground="White"
                        BorderThickness="1"
                        BorderBrush="#4A4A4A"
                        Padding="12,4"
                        Margin="6,0,0,0"
                        FontSize="12"
                        Cursor="Hand"
                        Click="ClearAllButton_Click">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border x:Name="border"
                                                Background="{TemplateBinding Background}"
                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                CornerRadius="3"
                                                Padding="{TemplateBinding Padding}">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="border" Property="Background" Value="#4A4A4A"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>
            </Grid>
        </Border>

        <!-- Variable list -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="4">
            <ItemsControl x:Name="VariableList">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="#252526"
                                Margin="2"
                                Padding="8,6"
                                BorderBrush="#3F3F46"
                                BorderThickness="1"
                                CornerRadius="2">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsChanged}" Value="True">
                                            <DataTrigger.EnterActions>
                                                <BeginStoryboard>
                                                    <Storyboard>
                                                        <ColorAnimation Storyboard.TargetProperty="Background.Color"
                                                                        From="#CCC900"
                                                                        To="#252526"
                                                                        Duration="0:0:0.6"/>
                                                    </Storyboard>
                                                </BeginStoryboard>
                                            </DataTrigger.EnterActions>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="80"/>
                                    <ColumnDefinition Width="100"/>
                                    <ColumnDefinition Width="60"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <!-- Variable name -->
                                <TextBlock Grid.Column="0"
                                          Text="{Binding Name}"
                                          FontFamily="Consolas, Cascadia Code, Courier New"
                                          FontSize="12"
                                          Foreground="#4EC9B0"
                                          VerticalAlignment="Center"/>

                                <!-- Register -->
                                <TextBlock Grid.Column="1"
                                          Text="{Binding Register}"
                                          FontFamily="Consolas, Cascadia Code, Courier New"
                                          FontSize="12"
                                          Foreground="#858585"
                                          VerticalAlignment="Center"/>

                                <!-- Value -->
                                <TextBlock Grid.Column="2"
                                          Text="{Binding Value}"
                                          FontFamily="Consolas, Cascadia Code, Courier New"
                                          FontSize="12"
                                          Foreground="White"
                                          VerticalAlignment="Center"
                                          MouseLeftButtonDown="Value_MouseLeftButtonDown"/>

                                <!-- Line -->
                                <TextBlock Grid.Column="3"
                                          Text="{Binding Line}"
                                          FontFamily="Consolas, Cascadia Code, Courier New"
                                          FontSize="11"
                                          Foreground="#858585"
                                          VerticalAlignment="Center"/>

                                <!-- Remove button -->
                                <Button Grid.Column="4"
                                        Content="✕"
                                        Width="20"
                                        Height="20"
                                        FontSize="12"
                                        Background="Transparent"
                                        Foreground="#858585"
                                        BorderThickness="0"
                                        Cursor="Hand"
                                        Click="RemoveButton_Click"
                                        Tag="{Binding}">
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Style.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Foreground" Value="#F14C4C"/>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- Empty state -->
        <TextBlock x:Name="EmptyMessage"
                   Grid.Row="1"
                   Text="Add variables to watch"
                   Foreground="#858585"
                   FontSize="13"
                   FontStyle="Italic"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   Visibility="Visible"/>
    </Grid>
</local:WidgetBase>
